import { expect } from "chai";

/**
 * TODO: Should be replaced with actual tests
 */
describe("Testing the testing", () => {
  it("Should pass this one", () => {
    expect(true).to.be.true;
  });
});
